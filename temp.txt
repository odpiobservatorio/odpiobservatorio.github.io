            if (criterio.clase=="clsCasos"){
                const l = operadores.operadorIgual(registro,criterio.field,criterio.value)
                condiciones.push(l)
            }else{
                const l = operadores.operadorAlguno(registro,criterio.clase,criterio.field,criterio.value)
                condiciones.push(l)
            }

   